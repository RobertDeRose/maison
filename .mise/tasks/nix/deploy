#!/usr/bin/env bash
# [MISE] quiet=true
# [MISE] description="Compatibility alias for system:deploy"
# [MISE] hide=true
set -euo pipefail
exec mise -C "${MAISON_HOME:-${MISE_PROJECT_ROOT:-$PWD}}" run "system:deploy" -- "$@"
