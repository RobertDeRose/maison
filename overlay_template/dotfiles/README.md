# Consumer dotfiles

Place consumer-owned dotfiles and templates under this directory. Add mappings in `config/mise/config.toml`.

Do not store passwords, tokens, private keys, or other secret values here; use the consumer's fnox provider instead.
