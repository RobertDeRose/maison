# OpenCode configuration

OpenCode is installed by Home Manager. No static preferences existed at migration time. Add future native OpenCode configuration files here instead of encoding ordinary settings as Nix values.

The repository-owned TypeScript workspace under `dotfiles/pi/extensions` is scoped to Pi extension validation. It does not
change OpenCode runtime configuration, installation ownership, or command behavior.
