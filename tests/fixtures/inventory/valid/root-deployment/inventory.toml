schema = 1

[users.operator]
username = "operator"
full_name = "Example Operator"
email = "operator@example.invalid"
github = "example-operator"

[hosts.root-bootstrap]
system = "aarch64-linux"
user = "operator"
profiles = ["base", "linux"]

[hosts.root-bootstrap.deploy]
enable = true
hostname = "root-bootstrap.example.invalid"
ssh_user = "root"
user_ssh_user = "operator"
repo_path = "/home/operator/.maison"
