schema = 1

[users.operator]
username = "operator"
full_name = "Example Operator"
email = "operator@example.invalid"
github = "example-operator"

[hosts.deploy-extra]
system = "aarch64-linux"
user = "operator"
profiles = ["base", "linux"]

[hosts.deploy-extra.deploy]
enable = true
hostname = "deploy-extra.example.invalid"
ssh_user = "maison-deploy"
user_ssh_user = "operator"
repo_path = "/home/operator/.maison"
unknown = true
