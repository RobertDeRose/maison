# Maison intentionally keeps the public macOS application policy empty.
# Add personal applications to a private overlay based on examples/terroir.
[settings]
system_packages.managers = ["brew", "brew-cask", "mas"]

[bootstrap.packages]
