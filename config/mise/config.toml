# Maison intentionally keeps the public user policy empty.
# Add personal tools and packages to a private overlay based on examples/terroir.
[settings]
experimental = true
env_shell_expand = true
lockfile = true
lockfile_platforms = ["macos-arm64", "linux-arm64", "linux-x64"]

[plugins]
nix = "https://github.com/jbadeau/mise-nix.git#e310c4c20e8273ba850298c10fbe97d0c858c300"

[tools]

[bootstrap.packages]

[env]
