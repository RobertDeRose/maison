# Maison keeps the public Linux user policy neutral.
# Consumer repositories may add personal packages and environment settings here.
[settings]
system_packages.managers = ["brew"]

[env]
LANG = "C.UTF-8"
LC_CTYPE = "C.UTF-8"
