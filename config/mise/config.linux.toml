# Maison keeps the public Linux user policy neutral.
# Add personal packages and environment settings to a private overlay based on examples/terroir.
[settings]
system_packages.managers = ["brew"]

[env]
LANG = "C.UTF-8"
LC_CTYPE = "C.UTF-8"
